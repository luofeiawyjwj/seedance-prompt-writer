@AGENTS.md

## Claude Code

If this repository is installed under `~/.claude/skills/seedance-prompt-writer/` or `.claude/skills/seedance-prompt-writer/`, invoke it as `/seedance-prompt-writer`.

When the user asks for a Seedance prompt, follow `SKILL.md` directly instead of summarizing this repository.
